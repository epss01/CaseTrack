{
  "permissions": {
    "additionalDirectories": [
      "C:/Users/admin/Downloads/Capstone_CaseTrack/CaseTrack_Vault/CaseTrack/wiki",
      "C:/Users/admin/Downloads/Capstone_CaseTrack/CaseTrack_Vault/CaseTrack/raw/sessions"
    ]
  },
  "autoMode": {
    "environment": [
      "$defaults",
      "### Org-wide",
      "**Organization**: None configured — this repo (epss01/CaseTrack) shows no evidence of a broader org; treat as an independent personal/capstone project",
      "**Cloud provider(s)**: None configured — AWS env vars present in .env.example are blank placeholders (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_BUCKET all empty), no active cloud usage found",
      "**Repository visibility**: PUBLIC — github.com/epss01/CaseTrack (confirmed via gh). Any push here is publishing; treat committed content as public.",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured — local .env files only (.env, .env.docker, .env.example, .env.xampp); .gitignore excludes .env, .env.backup, .env.production, /storage/*.key",
      "**Default / protected branches**: Default branch main (deployment-checkpoint only per CONTRIBUTING.md); gh reports no rulesets and no classic branch protection listed on main. develop is the working trunk per CONTRIBUTING.md.",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (epss01/CaseTrack, public) and its origin remote only — no additional orgs configured",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: Victim, respondent, and complainant records (names, ages, sectors, incident details) held in the local MariaDB `casetrack` database (port 3307) and any exports via /reports/export; audit_logs table; this is CHR Region VIII case data covered by RA 10173 — share only with cleared CHR/CaseTrack project audiences, never commit real case data to the public repo",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "### User-specific",
      "**Primary use of Claude Code**: software development (Laravel/PHP capstone web app — CaseTrack)",
      "**Trusted repo**: C:\\Users\\admin\\Downloads\\Capstone_CaseTrack\\CaseTrack (working directory) and its origin remote, https://github.com/epss01/CaseTrack.git. Repo is PUBLIC: only this repo's own work belongs there — confidential/case-simulation data, real CHR records, or content ported in from elsewhere is not cleared for commit/push here. CLAUDE.md and .claude/ must never reach main (stripped at promotion per CONTRIBUTING.md).",
      "**Org-specific CLIs**: None configured — project-specific: php must be invoked via absolute path C:/Users/admin/.config/herd-lite/bin/php.exe (not on PATH); composer via C:/Users/admin/.config/herd-lite/bin/composer.phar"
    ],
    "soft_deny": [
      "$defaults",
      "Bash(git push origin main:*) in epss01/CaseTrack — main is a deployment checkpoint per CONTRIBUTING.md and should only advance via explicit approved promotion from develop, not routine pushes"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "php \"$CLAUDE_PROJECT_DIR/.claude/hooks/guard-writes.php\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "php \"$CLAUDE_PROJECT_DIR/.claude/hooks/run-tests.php\"",
            "timeout": 180
          }
        ]
      }
    ]
  }
}
